spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 假设这是线上的数据库地址
    url: jdbc:mysql://192.168.1.100:3306/rbac_system_prod?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: prod_user
    password: ${DB_PASSWORD:}

# 生产环境坚决不配置 mybatis-plus 的 log-impl，避免敏感 SQL 和密码密文泄露